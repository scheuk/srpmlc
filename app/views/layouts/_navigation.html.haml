%ul.nav.pull-left
  %li{:class => active_navigation(root_path)}
    = link_to "Home", root_path, :class => 'brand'
  - if user_signed_in?
    %li{:class => active_navigation(sites_index_path)}
      = link_to('Sites', sites_index_path)
      - if current_user.has_role? :admin
        %li{:class => active_navigation(users_path)}
          = link_to('Admin', users_path)
/  - else
/    %li
/      = link_to('Sign up', new_user_registration_path)
%ul.nav.pull-right
  - if user_signed_in?
    %li{:class => active_navigation(edit_user_registration_path)}
      = link_to(current_user.email, edit_user_registration_path)
    %li
      = link_to('Logout', destroy_user_session_path, :method=>'delete')
  - else
    %li
      = link_to('Login', new_user_session_path)
