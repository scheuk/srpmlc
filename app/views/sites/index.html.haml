%h3 Sites
.span8
  %table.table.table-condensed
    %thead
      %tr
        %th Site Name
        %th supervisor
    %tbody
      - @sites.each do |site|
        %tr
          %td= link_to site.sitename, site_site_records_path(site)
          %td= site.supervisor

